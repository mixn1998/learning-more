{
  "name": "@learning-more/release",
  "version": "0.0.0-development",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "portable": "node dist/build-portable.js",
    "smoke": "node dist/portable-smoke.js",
    "test": "vitest run --root ../.. tools/release",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
