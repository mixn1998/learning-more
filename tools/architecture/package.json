{
  "name": "@learning-more/architecture",
  "version": "0.0.0-development",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "corepack pnpm --filter @learning-more/contracts build && vitest run --root ../.. tools/architecture",
    "typecheck": "corepack pnpm --filter @learning-more/contracts build && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@learning-more/contracts": "workspace:*",
    "yaml": "2.9.0"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "5.9.3"
  }
}
