# 周报默认收起实施计划

**目标：** 上周学习报告进入页面时默认收起，标题栏显示“+”；用户点击后展开并显示“−”，再次点击恢复收起。

## 实施与验证

- [x] 在交互回归测试中增加默认收起、符号状态及两次点击切换断言。
- [x] 移除周报 HTML 的默认 `open` 类，并将初始符号改为 `+`。
- [x] 运行交互回归及页面冒烟测试，确认日期筛选等相邻交互不受影响。
- [x] 将该默认状态同步到项目上下文和 UI 规则文档。
