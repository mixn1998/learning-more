# Learning MORE 2.0 当前态文档

本目录描述 2026-07-16 时点 Learning MORE 的实际项目状态，不维护开发历史，也不把计划、样稿或未接入接口写成已交付能力。

## 文档结构

- [01-功能设计层.md](./01-功能设计层.md)：用户能力、业务流程、状态规则、AI 与确定性程序的职责边界。
- [02-前端实现层.md](./02-前端实现层.md)：页面路由、功能组件、客户端状态、交互与共享 UI 实现。
- [03-后端架构层.md](./03-后端架构层.md)：进程拓扑、服务模块、HTTP 接口、AI 运行时、持久化与恢复机制。

## 核验口径

事实优先级如下：

1. 当前可访问的运行实例及其 HTTP 响应；
2. 当前分支的启动接线、路由、模块接口、领域模型和持久化实现；
3. 当前自动化测试、架构检查与静态检查；
4. 旧文档仅用于定位术语和线索，不作为事实来源。

文中使用四种状态：

- **已接入**：生产启动链已创建该能力，并有页面或 HTTP 路由可达。
- **已实现**：存在可执行实现和测试，但不一定进入当前产品主路径。
- **注册未接入**：存在类型、场景名或接口，但生产代码没有提交或调用路径。
- **测试夹具**：只在测试或视觉样例中出现，不能视为生产配置。

## 当前源码与运行状态

- 分支：`master`；文档口径以本目录随当前提交保存的源码为准，不单独固化易过时的短提交号。
- 仓库门禁：`corepack pnpm verify` 通过，包括格式、ESLint、类型、Schema、架构、等价性、221 个测试文件/860 项测试和全工作区构建。
- 架构检查：273 个 data key、0 个禁止依赖、0 个禁止画像写入；等价矩阵 77/77 通过。
- Web 构建产物包含严格的 `build-meta.json`。
- Windows 计划任务 `Learning MORE` 已安装、配置匹配且处于运行状态，Host 持续监督 Launcher 和 Server。
- 当前固定端口 `127.0.0.1:43119/43120` 由 Host 管理的活动 release 提供服务；Server readiness 为 `ready`，Runtime 与 served Web 的 `buildId`、`protocolVersion` 一致，并通过三方版本验收。
- 一键重连已经能够从当前工作区构建候选 release，经过 staging、activating 后切换活动实例；构建或验证失败时保留旧 release 和用户数据。
- 浏览器 runtime 套件没有在当前主线构建上重新执行：固定端口正由正式常驻实例占用，不能把“测试可发现”记作新的运行态通过结论。

## 维护规则

后续功能、前端或后端发生实质变化时，应直接更新对应层文档。临时实施计划、阶段报告和日期化设计稿不得再承担“当前事实”职责。
