# Learning MORE 2.0 当前态文档

本目录描述 2026-07-16 时点 Learning MORE 的实际项目状态，不维护开发历史，也不把计划、样稿或未接入接口写成已交付能力。

## 文档结构

- [01-功能设计层.md](./01-功能设计层.md)：用户能力、业务流程、状态规则、AI 与确定性程序的职责边界。
- [02-前端实现层.md](./02-前端实现层.md)：页面路由、功能组件、客户端状态、交互与共享 UI 实现。
- [03-后端架构层.md](./03-后端架构层.md)：进程拓扑、服务模块、HTTP 接口、AI 运行时、持久化与恢复机制。

## 核验口径

事实优先级如下：

1. 当前可访问的运行实例及其 HTTP 响应；
2. 当前分支的启动接线、路由、模块接口、领域模型和持久化实现；
3. 当前自动化测试、架构检查与静态检查；
4. 旧文档仅用于定位术语和线索，不作为事实来源。

文中使用四种状态：

- **已接入**：生产启动链已创建该能力，并有页面或 HTTP 路由可达。
- **已实现**：存在可执行实现和测试，但不一定进入当前产品主路径。
- **注册未接入**：存在类型、场景名或接口，但生产代码没有提交或调用路径。
- **测试夹具**：只在测试或视觉样例中出现，不能视为生产配置。

## 本次核验基线

- 分支：`master`
- 基线提交：`24a4088`
- 工作区：运行代码已提交；仅保留本轮安全激活设计、实施记录和当前状态文档更新。
- 运行现场：`127.0.0.1:43119/43120` 由一个旧实例占用。该实例的 Runtime 为 `ready`、build 为 `d2b9bf0ab403-w6d008f9ebe05`，但静态端尚无 `build-meta.json`，因此它不是本轮代码的三方版本验收实例。本轮未停止或接管该实例。
- 单元/组件/架构测试：全库 221 个测试文件、860 项测试全部通过；安全激活定向 seam 3 个文件/4 项、核心跨层 10 个文件/53 项、恢复矩阵 4 个文件/26 项全部通过。
- ESLint：通过。
- Prettier 检查：通过。
- 类型、schema、架构与等价性检查：全部通过；273 个 data key、0 个禁止依赖、0 个禁止画像写入，77/77 等价断言通过。
- 全工作区构建：通过，Web 产物包含 `build-meta.json`。

本次文档治理自身的校验结果：

- 当前 2.0 文档及保留索引通过 Prettier 检查；
- Markdown 相对链接无失效目标；
- 数据键同步测试通过；
- 等价矩阵 77 条全部通过；
- 浏览器 runtime 用例可正常发现，但因当前旧实例占用固定端口，未执行会启动第二套 Launcher/Server 的运行态用例；该限制不计作通过。

## 维护规则

后续功能、前端或后端发生实质变化时，应直接更新对应层文档。临时实施计划、阶段报告和日期化设计稿不得再承担“当前事实”职责。
