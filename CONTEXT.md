# Learning MORE 领域词汇

## 正式课程

用户确认最终候选大纲后才创建的一级学习实体。确认前不存在草稿课程。

## 大纲创建会话

正式课程创建前，用于学习起点评估、候选大纲生成和调整的可恢复 AI 对话切片。它不是课程。

## 候选大纲

AI 在大纲创建会话中生成的完整可确认快照，包含 Markdown、一个学科标签和一至三个主题标签。

## 确认版大纲

用户显式确认后保存的不可变大纲版本。后续修改创建新版本，不改写旧版本。

## 课程单元

从确认版大纲解析出的可点击课节定义。课程单元不包含预生成正文。

## 课节生命周期

课节在学习维度上的独立状态：未开始、学习中、已完成或已放弃。生命周期决定课节是否能够继续学习、生成 Review、进入推荐或计入完成统计；它不等同于是否已经安排学习日期。

## 排期状态

供课程规划、计划流和课程表使用的实时派生状态，而不是可写入的课节字段。对未完成且未放弃的课节：无排期日期为“待规划”；排期日在今天或未来为“已安排”；排期日早于今天为“已逾期”。已完成与已放弃课节不属于待排期列表。

## 知识链

确认版大纲以一条有序主逻辑链规定课节的教学责任，必要的边界、反例和补充概念作为分支依附于主节点。课堂按主链推进，分支不作为独立进度节点。节点完成表示相应教学责任已经完成且当前阻塞项已经处理，不表示系统对学习者作出掌握判定。

## 渐进式生成

AI 不在开课前生成并固化整篇课程正文，而是先形成覆盖本课核心知识点的内部教学路径，再依据学习者最新表达和当前理解障碍逐段生成后续讲解与互动。AI 可以动态改变顺序、深度、案例和互动方式；本次学习实际发生的完整对话构成课程正文。

## 课前热身

AI 在具体教学前按证据决定是否进行的起点校准。AI 先读取学习起点评估、确认版大纲和已完成 Review；现有证据不足时动态提问，证据充分时可以缩短或跳过。热身不使用固定问卷或固定轮数，其结果只用于调整本课解释深度、起点和案例，不固化为永久水平标签。

## 学习表现证据

学习者在解释、应用、比较、预测、纠错、举例或完成任务时自然呈现的真实回答与推理过程。它用于教学调整、Review、学习行为、画像和统计，不作为知识点通过或课程推进门禁。发现偏差后，AI 必须针对偏差换一种解释、例子或思考角度，但不要求学习者通过另一道题证明已经理解。

## 教学智能体

依据当前课节责任、教学状态、固定个性化视图和最近对话，自由决定下一段讲解、互动邀请、案例、纠偏与节奏的 AI 能力。教学智能体同时返回给学习者阅读的 Markdown 和前端不可见的教学状态指令；状态指令只推进教学账本，不直接写入 Review、全局用户档案或学习画像。智能体不得默认学习者已经理解，也不得用频繁盘问中断教学推进。每轮完成一个自然教学单元后，智能体或者发出有教学价值的回应邀请，或者交还“继续讲解”选择权；继续是系统事件，不构成学习者消息或学习证据。综合应用回应完成或被跳过后进入讨论答疑，学习者明确没有其他问题后才可输出最终课程总结并把课程标记为可关闭。

## 教学观察

教学观察器针对学习会话增量形成局部且带精确来源引用的语义记录。它记录已经发生的教学行为、学习者回答、推理、提问、纠偏、显式目标、跳过行为和仍待处理的开放项，但不决定知识点完成、教学阶段或课程闭环。教学观察是 Review、学习行为、画像与统计的原始行为来源之一，不是教学账本的推进者。

## 思维行为 Episode

从有效 `learner_reasoning_behavior` 教学观察条目幂等投影出的单次局部行为记录，保存自由语义摘要、课程/课节/会话/玩法情境、自发或教学引出标记和精确来源引用。它属于原始行为层，供课时/阶段 Review、审计和统计消费，不直接成为全局用户档案或学习画像证据。暂停学习事实在原始行为入口即被排除，任何层都不得把它计为行为证据。

## 会话聚合维度

课时或阶段 Review 在保持原有原始行为消费逻辑不变的同时，按“学习会话 + 抽象维度”生产的会话级聚合数据。每个聚合维度保留 Review 检查点及原始来源引用；同一会话的最终课时 Review 覆盖该会话先前阶段 Review 的聚合结果，不增加独立来源数。课程总 Review 可以消费同主题下的会话聚合生成主题级 Review 成果，但不得把自身再次写成画像行为证据。

## 动态思维行为维度

AI 对多个独立学习会话的会话聚合维度进行第二次语义归并后形成的跨会话稳定维度，包含自由名称、定义、纳入/排除信号和来源会话。“逻辑、关联、发散、结构、隐喻”等只是可能出现的例子，不是固定枚举、数据库列或永久雷达轴。不同课时中措辞不同但本质一致的维度必须归并；单一会话只能形成暂定模式。

## 思维行为分析快照

在明确时间、课程、课节和玩法过滤器下，对 active 会话聚合维度与全局多标签分类形成的可复算统计快照。它记录来源会话数、各全局维度计数/占比、课程/课节覆盖及局限。默认教学个性化和学习画像只消费无过滤、usable 且同一模式至少由两个真正独立学习会话支持的全局快照；定向分析不自动覆盖全局依据。

## 教学状态账本

按消息顺序归并教学智能体隐藏结构化指令形成的课节会话级可重建投影。账本只记录主链节点的 `pending / learning / completed / skipped`、互动状态、综合应用、讨论答疑、总结、交接方式和闭环状态，用于下一回合教学、前端进度和关闭判断。教学观察与“继续讲解”事件不推进账本。节点推进采用“必要讲解已完成 + 当前无未处理疑问或理解障碍”的标准；如果已经发出互动邀请，还需要该互动已回应或明确跳过。推进不使用回答正确次数或通过判定。

## 教学支线

学习者主动提出的、与整门课程相关但不属于当前课节责任的探索方向。教学智能体可以自然展开教学支线，但账本必须保留其课程主题关联和返回当前课节的锚点；支线内容不计为当前核心知识点覆盖，也不自动完成未来课节。支线成为新的主要学习目标时，由学习者选择继续、暂存后返回本课，或以后建立补充学习或新课，AI 不自动修改确认版大纲。

## 教学检查点快照

在有证据放弃或最终结课等受控业务检查点冻结的教学状态账本、观察引用和消息范围快照。阶段或最终 Review 继续消费该快照及其精确来源引用；Review 提交后再生产会话聚合维度。全局用户档案只消费会话聚合维度，不直接扫描整段原始对话，也不把课程总 Review 当作新的独立证据。暂停学习不会形成行为证据检查点。

## 用户档案候选证据

全局用户档案在单次大纲创建会话、学习会话、Review 或其他受控学习检查点中提炼的中性、局部且可追溯的语义证据。它可以记录学习者明确说出的目标、约束或偏好，也可以记录真实互动中出现的回答、推理、提问、纠偏和变化，但不得提前上升为稳定用户结论。用户档案候选证据与原始来源绑定并经过敏感信息净化；原始对话全文不直接进入全局用户档案消费输入。单条候选证据只能作为候选观察，不能独立生成正式画像洞察或永久个性化标签。

## 证据来源组

用于识别用户档案候选证据独立性的来源边界。同一学习会话及其阶段或最终 Review、同一课程的课时 Review 与由其汇总形成的课程总 Review，虽然可能产生多个文本或数据对象，但不能被当作彼此独立的重复证据。正式画像洞察必须由多个具有一定独立性的来源组共同支持。

## 复合行为证据链

由多个相互关联且具有一定独立性的用户档案候选证据共同形成的洞察支撑关系。证据链可以跨课节、跨课程或跨时间，并必须保留会限制、反驳或缩小结论适用范围的证据；它不是固定分析栏目，也不要求预设洞察类型。

## 全局用户档案

后台长期维护并供学习画像、教学个性化和其他受控个性化方案使用的权威数据域，不等于用户可见的学习画像。它消费各学习会话的聚合维度，对语义本质一致的维度再次抽象和归并，保存跨会话稳定维度、来源会话计数、学习统计快照、指标时间序列、来源索引、失效关系和档案版本。只有至少两个真正独立学习会话共同支持的模式才能进入正式画像结论和个性化隐式上下文。运行诊断、页面访问、暂停事实和 AI 服务遥测不属于全局用户档案；学习画像结果也不得反向成为档案事实。

## 学习画像

读取全局用户档案消费快照并自由生成用户可见分析结果的功能模块，包括自由标题、自由摘要、数量不定的洞察正文及其复合行为证据链。学习画像拥有自己的生成任务和不可变版本，但不维护长期学习事实、用户档案候选证据或个性化标签；它不能把生成结果反向写入全局用户档案。

## 统计事实快照

全局用户档案中某个统计数据在特定口径版本、时间窗口和数据游标下的可重建结果。快照必须携带数据键、口径版本、数值、单位、时间窗口、维度、样本量、排除项、完整性和计算时间；画像 AI 可以读取全部学习统计快照，但前端历史统计只选择其中已确认展示的数据。

## 指标时间序列

同一学习统计数据按本地自然日、周、月或其他明确窗口保存的连续统计事实快照。它用于让画像 AI 比较近期、中期与长期变化，不允许用技术遥测、页面访问或缺乏明确口径的数据补齐序列。

## 有效学习证据检查点

用于判断放弃课节时应保留还是回收原始学习会话的内部语义标记，不等同于知识点完成。AI 完整输出实质知识讲解，或学习者对热身、互动邀请作出实质回应，或提出与课节知识相关的具体问题、判断或反思后，系统写入 `evidenceCheckpoint`。欢迎语、导航与流程说明、“开始吧”等无知识信息回复、只有邀请而尚无回应、被停止或技术中断的残缺 AI 输出，以及页面停留、按钮点击和时长均不构成检查点。判定不依赖字数或时长；无法确定时优先保留会话，避免误删真实学习记录。

## 关键节点互动

AI 在适合激活思考、暴露理解障碍或决定后续教学路径时邀请学习者回应的互动。互动不是检测，也不按固定段落、知识点数量或轮次触发；AI 可以先完整讲清紧密关联的内容。学习者可以回答邀请、提出自己的相关思路、随时打断提问或明确跳过互动，AI 回应后自然回到主线。

## 跳过互动

学习者暂不回答当前互动邀请并要求继续、不展开或跳过的行为。AI 不重复逼问，简短小结后可以推进；跳过不代表掌握或未掌握，只表示本次互动没有继续。知识点必要讲解尚未完成或仍有未处理疑问、误解和不解时，不能借“跳过互动”直接完成知识点。

## 对话追加原则

学习会话中的已发送消息保持原样，学习者和 AI 均通过新增消息纠正、补充或换一种讲法，不编辑、删除或重新生成已经完成的历史消息。因技术故障而未完成的 AI 回复可以继续生成或重试，此行为用于恢复同一次回复，不产生替代历史。

## 停止生成

学习者主动中断当前 AI 流式回复的行为。已经呈现的部分保留为未完成回复，不能单独视为已完成教学；学习者可以补充新问题或继续原回复。停止生成只中断当前输出，不暂停学习会话，也不结束本课。

## 学科标签

AI 识别的课程主要学科或领域。每个候选大纲和确认版大纲恰好包含一个。

## 主题标签

AI 识别的课程实际聚焦主题。每个候选大纲和确认版大纲包含一至三个。

## 课程模式

课程从标准入口或某个玩法入口发起的来源类型。它不表示思维模式，也不直接决定大纲形状。

## 玩法壳

位于主页左侧九模式入口栏中的八个玩法课程创建身份。八个玩法壳彼此同级，并与第一张“标准模式”卡使用相同 UI 规格；点击入口卡只选择待提交的 `courseMode`，不包含旧玩法业务逻辑，也不立即创建 OutlineSession 或正式课程。

## 玩法隐藏上下文

玩法入口提供给 AI 的整体、非强制玩法意图。正式教学时，`courseMode` 与 `playIntent` 都位于第二层“课程与本课契约层”：前者是课程来源身份事实，后者是第二层中的软性教学机会偏向。`playIntent` 只在多个教学动作都合理时参与择优，不设置数值权重、逐回合配额或固定激活阶段；标准模式不携带它。它帮助 AI 理解该玩法希望带来的学习体验和思考方向，但不对应课程模块、阶段、课节类型、流程节点或输出形式。AI 可以根据学科知识依赖、学习者回答和最新证据自由决定课程结构与教学路径；玩法意图不显示为标签、不直接写入大纲正文，也不能覆盖用户当前意图、本课责任和教学账本事实。

## 玩法设计观察面

用于设计者辨认玩法差异的四个观察角度：核心体验、优先关注、价值信号和 Review 视角。它们只服务于玩法定义和质量检查，不被编译为课程结构、执行清单或 Prompt 字段。玩法层不维护“可选方法”、推荐技法或方法库，教学方法由 AI 根据主题、材料、学习起点和实时互动自主决定。

## 标准模式

`courseMode = standard` 的通用课程创建入口，不属于八大玩法。主页九模式入口栏中固定排在第一位，使用与玩法卡相同的 UI 规格，并承担从任一玩法选择恢复到标准模式的功能；该操作不使用“重置”命名，也不清空尚未提交的主题。

## 渐进式模式身份延续

课程的 `courseMode` 视觉身份从主页、建档、修改大纲、正式学习延续到 Review 与历史，但强度逐步收敛：创建阶段识别最强，学习阶段适度收敛，归档和 Review 只保留稳定标识。模式色表示课程来源身份，成功、警告、错误、已放弃和只读继续使用独立全局状态色。

## 课时 Review 弹窗

最终课时 Review 首次成功写入且课节完成后立即出现的大尺寸完整 Markdown 弹窗。弹窗本身只读，只提供“返回课程大纲”和“查看课节记录”；后者进入课节记录并默认定位“课时 Review”Tab。

## 阅读材料

用户在阅读研讨玩法中上传并确认用于建课的原始文本文件。系统保存原文件引用和解析快照；AI 以材料内容和章节结构作为评估与大纲生成证据，但不能把解析推断冒充原文。

## 材料章节映射

阅读研讨确认版大纲与原材料章节、标题或页码范围之间的可追溯关系。它保证原文覆盖和课节定位，但不要求一章对应一课；AI 可以根据知识依赖合并、拆分或调整教学顺序。

## 通用课程不变量

所有标准课程和玩法课程共同遵守的生命周期与数据边界，例如显式确认大纲、对话不可改写、Review 单一且最终不可变、课节状态机和课程关闭规则。只有这些跨玩法规则属于硬约束；玩法导向不得新增一套状态机或破坏这些不变量。

## 头脑风暴

以扩大可能性空间、发现不同方向和隐藏假设为主体验的课程模式。它不要求固定的发散或收敛流程。

## 论证交锋

以检验主张、证据、前提和强反方为主体验的课程模式。它不等同于每课进行辩论。

## 案例研习

以在具体情境和真实约束中判断，并提炼机制与迁移边界为主体验的课程模式。它不要求每课都有案例。

## 商业洞察

以识别价值创造、传递、获取和机会验证路径为主体验的课程模式。它不绑定固定商业分析框架。

## 流程拆解

以理解复杂任务的步骤、依赖、异常和动态调整为主体验的课程模式。它不要求大纲成为步骤清单。

## 决策分析

以在目标冲突和不确定性中形成可解释、可调整的行动判断为主体验的课程模式。AI 不代替学习者承担决定。

## 交叉探索

以切换多个领域视角、检验概念迁移并重构问题为主体验的课程模式。它不要求固定学科数量。

## 阅读研讨

以原始材料为证据进行理解、讨论、批判和输出的课程模式。它支持材料上传和章节映射，但不要求逐段精读或一章一课。

## 学习会话

用户打开某个课程单元后创建的独立 AI 对话。每个课节在完成前只有一条原始学习会话；暂停学习时保持可写，形成有效教学或互动证据后放弃课节时冻结，恢复已放弃课节时解除同一会话的冻结。若放弃时尚未形成任何有效教学或互动证据，已创建的空会话被删除。课节完成后原始学习会话永久只读，继续讨论会创建补充学习会话。

## 学习会话写入权

同一原始学习会话在任一时刻只允许一个页面实例拥有的排他写入资格。拥有写入权的窗口可以发送消息、接收 AI 渐进式回复并累计实际学习时长；其他同时打开该课节的窗口只读展示最新内容，不累计时长。学习者可在只读窗口点击“切换到此窗口”接管写入权，接管成功后原窗口立即停止计时并转为只读，以避免重复回复、消息乱序和重复计时。

## 课节版本绑定

原始学习会话在首次开始时永久绑定当时的 `LessonDefinition` 和 `OutlineVersion`。课程后来发布新版大纲时，暂停或已放弃课节的原会话不会被新版课节静默替换；恢复学习始终继续原版本、原课节和原会话。若新版大纲已调整、拆分、替换或移除该课节，恢复导航必须提示版本差异，并允许学习者返回当前大纲另选课节；另选新版课节属于开始新的课节，不属于恢复原会话。原会话及最终 Review 始终归属于其原始课节定义和大纲版本。

## 已完成课时锚点

`learning.progress = completed` 的课时在后续每一版确认大纲中都必须沿用原 `LessonDefinition`，其课义、完成状态、学习会话和最终 Review 永久冻结。修改大纲时，过去版本上下文必须同时提供历史大纲生成对话的压缩记录，以及逐节标为“用户已完成”的冻结大纲概要；新版候选不得删除、替换或改写这些课时。

## 学习中

课节已经开始但尚未完成、也未被放弃的统一生命周期状态。学习者暂停学习时保留当前可写学习会话，课节继续处于学习中；再次进入时恢复原会话，不生成 Review。

## 课程闭环

AI 根据教学目标和学习者在互动中的真实回答判断本课目标是否已经达成，并提出结束或继续深化的建议。AI 不能自行结束课程；只有学习者显式确认“结束本课”后，学习会话才归档并进入 Review 生成。

## 结束本课

学习者主动结束当前学习会话的显式行为，在学习过程中始终可用。课程已经闭环时进入正常结束；课程尚未闭环时按放弃课节处理。AI 可以建议结束并说明依据，但不能隐藏、禁用或代替学习者执行该行为。

## 暂停学习

学习者暂停当前教学但保留学习进度的显式或系统触发行为。手动点击暂停、关闭页面、离开课程页或网络断开都会停止计时并保存原始学习会话；页面进入后台时立即停止计时，回到前台后自动恢复。暂停不冻结会话、不生成 Review，也不产生课节完成事实；恢复学习时继续同一会话。

## 实际学习时长

学习会话期间，同时满足“页面前台可见、当前窗口持有会话写入权且未暂停”的学习区间累计值。进入后台、关闭、断线、暂停、放弃或写入权转移时立即关闭当前计时区间；阅读和思考时间均计入。有有效证据的会话在暂停、放弃和恢复后继续累加，最终完成时以全部有效区间之和进入完成统计，并按最终完成日期进入学习日历和周报。已放弃未完成期间仅在所属课节记录中展示，不计入完成统计。无有效证据而删除的空会话同时丢弃其短暂停留时长，恢复后从零开始。课前预估时长只用于排期和预期提示，不能覆盖实际学习时长。

## 提前结束

学习者在本课教学责任尚未全部完成时显式结束学习。AI 必须先列出尚未完成或未经验证的核心知识点；确认后按放弃课节处理并停止计时。已经形成有效教学或互动证据时，冻结当前会话并生成或更新阶段 Review；尚无有效证据时，删除已创建的空会话且不生成 Review。两种情况都将课节标记为已放弃。

## 放弃课节

学习者明确将尚未完成的课节移出当前学习路径的可逆行为。放弃不等于完成，也不再参与推荐、继续学习入口、课程排期或计划流。已有有效教学或互动证据时，冻结原始学习会话并生成或更新当前唯一的阶段 Review；从未开始，或虽已开始但尚无有效证据时，不保留学习会话且不生成 Review。后一种情况恢复后回到未开始；前一种情况在导航中点击“恢复学习”后解除原会话冻结并回到学习中，也可先通过“查看记录”打开只读学习对话与阶段 Review。

## Review

每个课节任何时刻最多只有一份 Review。首次需要生成 Review 时创建稳定的 `reviewId`；尚无有效教学或互动证据的放弃不创建 Review。Review 是供用户阅读并存档的完整 Markdown，包含知识要点小结和学习评价；未完成时表现为阶段 Review，完整闭环后由最终课时 Review 覆盖。所有更新先生成完整候选内容，再原子覆盖同一 `reviewId`；失败时保留覆盖前内容，不保留 Review 版本历史，也不从 Markdown 之外复制一套结构化正文。

## 阶段 Review

课节已形成有效教学或互动证据、尚未闭环而学习者提前结束并放弃课节时生成的当前 Review。它综合此前 Review 与本次冻结会话，记录累计已经讲解的内容、自然呈现的学习表现以及尚未完成的核心知识点和开放问题。无有效证据时不生成阶段 Review。再次放弃时，新阶段 Review 原子覆盖旧阶段 Review；它不产生课节完成事实，也不作为其他课节默认继承的课节级记忆。

## 最终课时 Review

课节完整闭环后生成的最终 Review。它综合当前阶段 Review 和最后一次学习会话中的真实教学与互动证据，形成该课节的最终知识小结和学习表现评价，并原子覆盖阶段 Review。覆盖成功后只保留最终课时 Review；它成为其他课节与后续课程默认继承的唯一课节级权威上下文，并从成功写入时起永久不可修改、覆盖或重新生成。补充学习只保留新的独立对话，不产生或改变课时 Review。

## 课程关闭事务

学习者在课程闭环后确认结束，到最终课时 Review 成功写入之间的可恢复事务。系统先持久化结束意图、停止计时并冻结原学习会话，再以同一结束快照和 `reviewId` 幂等生成 Review；内部可使用 `closing` 表示处理状态，但不增加第五种用户可见课节生命周期。只有 Review 原子写入成功后才产生完成事实。失败时保留原对话和已有阶段 Review，允许重试；学习者也可选择恢复学习，取消结束意图并解除原会话冻结。服务重启后必须能够继续未完成的关闭事务。

## 课程总 Review

整门课程关闭后，由 AI 在课程学习档案层级生成的主题总结，不属于任何单一课时 Review。它以关闭时锁定的最终大纲、已完成课节的最终 Review 和已放弃课节现存的阶段 Review 为固定输入，包含主题核心知识线索总结、学习者总体学习表现和推荐扩展课程。课程关闭不等待总 Review；生成失败不撤销关闭，并允许使用同一输入快照重新生成。首次成功写入后课程总 Review 永久不可修改或重新生成，补充学习对话不会触发同步更新。入口文案为“查看主题总结”，只出现在课程学习档案页面；单课 Review 弹窗不展示该入口，也不展示下一课推荐。

## 课程关闭

当前最终确认版大纲中的全部有效课节均已处置后进入的不可逆状态。“已完成”和“已放弃”都属于已处置；只有“未开始”或“学习中”会阻止关闭。全部有效课节均已完成时自动关闭；若存在已放弃课节，则课程仅具备关闭资格，须由学习者在查看放弃清单和不可恢复提示后显式确认关闭，确认前仍可恢复。被新版明确移除或替换的未完成旧课节不参与关闭条件；已完成课时作为锚点必须沿用，并直接满足新版中的对应条件。关闭时锁定最终确认版大纲，不再允许修改大纲、追加课节、恢复已放弃课节或重新打开课程；后续深化或扩展必须创建一门新课程，并通过来源关系关联原课程。关闭课程仍可查看课程档案、课节记录和课程总 Review。

## 课程永久删除

针对单个正式课程的不可逆级联删除，不同于课程关闭。删除后同时清除课程档案、全部学习与补充会话、Review、排期、计划流、课程贡献的历史统计事实及学习画像证据；随后从剩余事实重建统计并触发画像重算。风险确认仅要求用户点击“永久删除”，不要求输入课程名称。画像重算失败不撤销删除，但不得继续展示已删除课程的证据或结论。

## Repository

领域模块用于读取和保存聚合的持久化 interface。初期由本地文件 adapter 实现，后续可替换为数据库 adapter。
