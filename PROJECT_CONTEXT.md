# Learning MORE 项目上下文与开发交接

> 更新时间：2026-07-13
> 项目根目录：`D:\workspace\Growth OS\Learning MORE`
> 旧项目参考：`D:\workspace\Growth OS\IKM\learning-demo`
> 用途：让新的 Codex 对话或开发者快速进入项目语境。详细规则以本文链接的专项文档为准。

## 1. 一句话定位

Learning MORE 是一个以 AI 实时对话为核心的个人学习系统。系统重点不是用大量字段和模板约束课程正文，而是管理 AI 生成的高质量课程、学习对话、Review、课程记忆、排期和学习档案。

## 2. 当前阶段

项目处于绿地重构的“架构与详细实施计划已批准、等待开始编码”阶段：

- 新目录已建立；
- 课程创建、课程学习和 Review 核心规则已经形成独立规格；
- 基础模块已整理功能等价与回归基线；
- 主要页面已有静态 HTML 最终视觉 sample；
- UI 资源已经完成按功能域迁移与全量样稿改造；27 个当前页面已通过桌面、平板、移动端控件几何、模块几何、文字与间距、控件接线和页面加载回归；22 组关键交互路径全部通过；
- 五部分程序架构（系统、Module/Interface、数据与任务、运行、测试）已经批准并写入正式架构文档；
- 主实施计划和八个阶段详细实施计划已经完成，包含文件地图、测试先行步骤、验收命令、阶段门禁和提交边界；
- 尚未创建生产前端、后端、共享合同或正式数据目录；
- 尚未开始生产代码；下一步从《实施计划-01-工程基座与共享合同》执行。

八个玩法功能规则已经形成正式设计稿，采用“通用课程引擎 + 整体玩法意图”：复用标准课程的 OutlineSession、候选大纲、正式学习、Review、课程关闭和计划流状态框架。四类内容仅是设计者辨认玩法差异的观察面，不得成为运行时字段、课程模块、阶段、课节类型、固定 Prompt、执行步骤或必交产物。玩法层不再维护“可选方法”、推荐技法或方法库；AI 必须根据主题知识依赖、材料结构和学习证据自由决定大纲、教学路径与具体方法。详细规则见 `docs/八大玩法功能规则.md`。

不要把 `docs/UI视觉预览` 中的 HTML 当成生产代码。它们是当前交互与视觉决策样例，正式实现需要用 React + TypeScript 重写。

## 3. 重构原因

旧项目的基础系统具有保留价值，但原课程学习流受到过多固定字段、Prompt 约束和模板控制，导致 AI 课程输出僵硬、质量下降。新项目采取整体重构而不是继续在旧代码上叠加修改，以避免课程状态、历史、排期、画像和 AI 服务逻辑继续耦合。

总体迁移原则：

- 基础模块保持功能与行为等价，可以优化内部实现；
- 课程创建与学习流按新领域模型重写；
- 不迁移废弃功能和垃圾代码；
- 旧项目只作为行为参考，不作为新架构骨架。

## 4. 已确认技术方向

- 前后端统一 TypeScript；
- 前端：React + Vite；
- 后端：Node.js；
- 前后端共享领域合同；
- 初期使用全新的本地文件数据目录；
- 所有持久化通过 Repository seam，预留数据库 adapter；
- AI 对话采用实时流式 Markdown；
- AI Provider、本地服务重连、自愈和状态切换需要复现旧项目能力。

## 5. 设计文档地图

阅读顺序建议：

1. [领域词汇表](./CONTEXT.md)
2. [基础模块功能等价清单与回归基线](./docs/基础模块功能等价清单与回归基线.md)
3. [课程创建通用流程与功能逻辑规则](./docs/课程创建通用流程与功能逻辑规则.md)
4. [课程学习与 Review 功能逻辑规则](<./docs/课程学习与 Review 功能逻辑规则.md>)
5. [八大玩法功能规则](./docs/八大玩法功能规则.md)
6. [主页与九模式建档工作台整合设计](./docs/主页与九模式建档工作台整合设计.md)
7. [UI 功能域分类、九模式视觉延续与课时 Review 弹窗设计](./docs/UI功能域分类、九模式视觉延续与课时Review弹窗设计.md)
8. [UI 视觉方案与最终稿清单](./docs/UI视觉方案与最终稿清单.md)
9. [历史统计与学习画像数据源定义清单](./docs/历史统计与学习画像数据源定义清单.md)
10. [学习画像数据抓取与分析策略规则](./docs/学习画像数据抓取与分析策略规则.md)
11. [教学范例集说明与采样框架](./docs/教学范例集/教学范例集说明与采样框架.md)
12. [Learning MORE 程序架构设计](<./docs/架构方案/Learning MORE 程序架构设计.md>)
13. [Learning MORE 详细实施计划](<./docs/架构方案/Learning MORE 详细实施计划.md>)（再按其中链接进入八个阶段计划）

如本文摘要与专项规格冲突，以最新专项规格和 `CONTEXT.md` 为准。

## 6. 不可回退的产品共识

### 6.1 课程创建

- 主页直接提供“想学什么”输入框，不再保留“创建档案”按钮；
- 主页顶部输入框默认创建 `standard`；左侧入口栏采用九张同规格大间距卡片，第一张为“标准模式”，其余为八大玩法。“标准模式”卡同时承担恢复 `standard` 的作用，不另设重置按钮。点击入口卡只为同一输入框选择 `courseMode`，不创建会话；提交后进入无玩法栏、无重复主题输入框的全宽统一建档工作台；阅读研讨选中时输入区额外显示材料上传入口；
- 用户只输入自然语言学习主题；
- 不再填写学科、阶段、时长、思维模式、知识范围等旧字段；
- 提交后进入 AI“学习起点评估”对话；
- 有效提交后立即进入建档工作台，不等待 OutlineSession 或首轮 AI 回复完成；工作台先展示产品固定的起始说明、用户刚提交的原始主题和“正在思考中…”，服务端返回后再以权威会话快照对齐；
- 评估对话可以中途退出并恢复，但在确认大纲前不创建正式 Course；
- AI 证据充分后生成完整 Markdown 候选大纲；
- 建档对话的每次发送都先即时显示用户消息并清空输入框，再显示“正在思考中…”；候选大纲按钮在点击后、服务端受理前也必须立即进入可见忙碌状态；受理失败时恢复输入并提供可重试提示；
- 用户可继续对话调整，显式确认后才创建正式课程和课节列表；
- AI 自动识别一个 `disciplineTag` 和 1–3 个 `topicTags`；
- 思维模式标签系统已彻底废弃；
- 确认版大纲不可变，修改大纲创建新版本；课程关闭后不再允许修改大纲。

### 6.2 玩法入口

- 灵感放送玩法彻底移除；
- 左侧共有八个同级课程创建壳：头脑风暴、论证交锋、案例研习、商业洞察、流程拆解、决策分析、交叉探索、阅读研讨；
- 八个玩法与通用课程创建流同层，通过整体玩法意图影响 AI 对学习任务的理解，不定义专属课程流程；
- 四类玩法导向只是设计观察面，不能映射为课程模块、阶段、课节类型、Prompt 字段或执行清单；所有玩法共享通用课程不变量和同一状态机；
- 八个玩法的核心体验、关注方向、价值信号、Review 视角及负向边界已经写入《八大玩法功能规则》；玩法定义不包含方法清单；
- 阅读研讨已确认支持 PDF、TXT、Markdown 等材料上传；解析结果参与学习起点评估，并按原文章节生成带章节映射的候选大纲。章节可因教学需要合并或拆分，但不得机械一章一课或静默遗漏重要范围；无法提取的扫描版或加密 PDF 不得假装解析成功。

### 6.3 正式大纲与课节入口

- 所有确认版课节均可自由点击；
- 系统只显示“推荐下一课”，不锁课、不警告用户选择其他课节；
- 每个课节必须在大纲中包含核心知识点；
- 未开始课节先打开核心知识点导航，点击“开始学习”才创建会话；
- 新课默认继承学习起点评估摘要、绑定版本大纲和当时全部已完成课节最终 Review；
- 其他课节原始对话全文不进入上下文。

### 6.4 正式学习

- AI 不预生成整篇课程正文，采用渐进式实时教学；
- AI 可以按证据决定是否进行课前热身；
- 讲解、互动、纠偏和验证由 AI 动态安排，不固定问题数和轮次；
- 不把“讲过了”当作“学会了”；
- 用户可以跳过某次互动，但跳过不构成掌握证据；
- AI 可以建议课程闭环，只有用户可以执行“结束本课”；
- AI 内容无整条气泡，用户内容使用右侧气泡，不显示发送者名称；
- 用户点击发送后必须立即看到自己的消息和“正在思考中…”，不得等 AI 完整回复后才一次性出现整轮对话；首个流式片段到达后由真实 Markdown 替换思考占位；
- 已完成消息只追加，不允许编辑或重新生成；技术中断的同一次回复允许续接；
- 用户可停止当前 AI 流式回复，残缺输出不构成完整教学证据。
- 本地乐观消息、固定起始说明、思考占位和按钮忙碌态只属于当前页面的临时视觉状态；它们不得写入 Repository、历史统计、全局用户档案、画像候选证据或复合证据链。只有服务端确认的规范消息与领域事实可以进入后续数据链。

### 6.5 课节状态

用户可见状态只有：

- `not_started`：未开始；
- `in_progress`：学习中；
- `abandoned`：已放弃；
- `completed`：已完成。

统一术语为“暂停学习”，不得重新引入“待继续”。

- 暂停保持 `in_progress`，停止计时，不冻结会话、不生成 Review；
- 有有效证据后放弃：冻结原会话并生成阶段 Review；
- 无有效证据时放弃：删除空会话，不生成 Review，恢复后回到未开始；
- 恢复已放弃课节解除同一原会话冻结，不创建续学会话；
- 同一课节同一时刻只有一个窗口拥有写入和计时权。

### 6.6 Review

- 每课任何时刻最多一个稳定 `reviewId`；
- 阶段 Review 可在最终完成前原子覆盖，不保留版本时间线；
- 最终课时 Review 包含本课要点小结和学习者互动表现评价；
- 最终 Review 首次成功写入后永久不可修改、不可纠错、不可重新生成；
- Review Markdown 是唯一权威正文；
- 最终 Review 失败时不完成课节，可以同快照重试或恢复学习；
- 单课 Review 弹窗只有“查看课节记录”和“返回课程大纲”。

### 6.7 补充学习

- 课节完成后可以发起补充学习；
- 补充学习创建独立会话并保留完整对话；
- 不改写原始学习对话；
- 不生成或修改课时 Review；
- 不更新课程总 Review；
- 课节记录顶层仍为“学习对话 / 课时 Review”；已放弃且有有效证据时，同一记录页以阶段状态展示冻结对话与“阶段 Review”，不单列独立页面或第三个顶层 Tab；
- “学习对话”内部左侧按时间排列原始学习对话和各次补充学习，右侧切换对应只读内容。

### 6.8 实际学习时长

- 只累计前台可见、持有写入权且未暂停的区间；
- 后台、离页、断线、暂停、放弃和写入权转移立即停止计时；
- 有证据会话跨暂停、放弃和恢复累计；
- 无证据空会话删除时清除短暂停留时长；
- 已放弃未完成时长不进入完成统计；最终完成后按完成日期一次性进入。

### 6.9 课程关闭和课程总 Review

- 最终大纲中只有 `not_started` 或 `in_progress` 会阻止关闭；`abandoned` 不阻止；
- 全部有效课节完成时自动关闭；
- 存在已放弃课节时采用混合规则：课程显示“可关闭”，用户查看放弃清单并确认后关闭，确认前仍可恢复；
- 课程关闭不可逆，不能改大纲、追加课节、恢复放弃课节或重开；
- 课程先关闭，再异步生成课程总 Review；失败不撤销关闭，支持固定快照重试；
- 课程总 Review 输入为最终大纲、已完成课节最终 Review、已放弃课节现存阶段 Review和无 Review 放弃事实；
- 课程总 Review 包含主题核心知识线索、总体学习表现和推荐扩展课程；
- 首次成功后永久不可修改或重新生成；补充学习不触发同步更新。

## 7. 基础模块保留范围

以下能力需要在新项目中保持旧项目功能逻辑等价，内部代码允许重写和优化：

- 主页总体结构、玩法栏和周课程表；
- 计划流创建、预览、排期和生命周期管理；
- 历史统计；
- 学习日历，只展示实际已完成课节；
- 学习画像；
- 上周学习报告，以 `Asia/Shanghai` 的周一 00:00 至下周一 00:00 为完整自然周，每周一 00:00 自动冻结并生成上一周成果概括；报告只消费完成课节、领域、标题和一句话摘要，不超过 300 字且不输出下周建议；失败按持久化退避时间自动重试，历史错误窗口和可精确识别的旧机械摘要自动重建，空周不调用 AI；进入回顾页时周报默认收起，点击标题栏以 `+ / −` 展开或收起；
- AI Provider 状态、切换和失败提示；
- 前后端服务一键重连、本地服务自愈和前端版本同步。

课程学习流变化引起的关联接口必须重新设计，不能直接照搬旧接口或旧状态枚举。

## 8. 当前视觉样例

权威目录：`docs/UI视觉预览`

样稿已按设计系统、主页、课程创建、规划、学习、Review、历史和运行服务分类。唯一索引为 `docs/UI视觉预览/README.md`；旧 `当前最终稿` 目录已经完成迁移并删除，不再作为有效资源入口。

重点入口：

- `01-主页与全局导航/主页.html`；
- `02-课程创建与大纲/标准模式建档.html` 及 `八大玩法建档/` 下八张独立样稿；
- `02-课程创建与大纲/正式课程大纲.html`、`修改大纲.html`；
- `03-课程规划与排期/课程规划.html`、`计划流向导与管理.html`；
- `04-课节学习/` 下导航、恢复和正式学习会话；
- `05-Review与学习档案/` 下课时 Review 弹窗、课节记录、周回顾和课程主题总 Review；
- `06-历史统计与学习画像/历史统计.html`、`学习日历.html`、`学习画像.html`；
- `07-系统运行与自愈/接口状态与本地服务自愈.html`。

视觉基调：暖白纸张、浅米灰背景、陶土色主操作、低饱和绿色成功状态、轻边框与克制阴影。桌面保持信息密度，移动端恢复自然单列。

当前关键 UI 规则：

- 主页组件布局保持旧项目结构并补齐周切换、返回当周、模式选择、导航弹窗等样稿交互；
- “继续学习”查询未关闭课程：一门时直达课程大纲；两门及以上时打开按最近学习时间排序、可按学科/领域与玩法模式筛选的课程选择窗口，含学习中课节者轻量突出；选择课程或大纲内“切换课程”均只进入课程大纲，不直接跳转课节；
- 历史统计已选择 A“分层归档”作为深化方向：顶部展示核心事实，中部展示趋势与分布，底部提供历史课程清单式回顾；课程清单上方直接并列状态、学科、来源模式与排序下拉项，不提供搜索框、主题筛选、总筛选按钮或常驻筛选侧栏；该页面不展示学习画像推断；
- 历史功能导航为“历史统计 / 学习日历 / 学习画像”三个同级 Tab 与三个视图；学习日历不是历史统计的子 Tab；
- 学习画像当前视觉样稿已确认采用自由标题与摘要、统一的纵向编号洞察卡、每条洞察可展开的复合行为证据链，以及列表末尾“可继续生成”的虚线占位；样稿正文只使用相同的通用占位内容，不展示任何具体画像结论模板；
- 学习画像洞察卡不得使用固定高度或最小正文高度，卡片随 AI 输出内容自然增长；固定的只有编号栏与正文内边距、标题与正文间距、段落行距和列表项间距，编号栏随整卡自动拉伸；
- 历史统计只展示可复算事实；“提问总数”“每课平均提问数”和“有效回复数”已废弃，互动事实统一为结构化关键互动发起、回应、跳过、有互动课节占比和回应率；
- 全部学习统计无论前端是否展示，都必须以累计值、时间序列、时间窗口、样本量、排除项和来源游标同步进入全局学习档案；运行遥测、页面访问、Provider、延迟、错误和写入权细节禁止进入学习画像；
- 学习画像采用“全局学习档案 + 画像候选证据 + 受控 Artifact + 上一版画像”的输入结构；候选证据只保存中性摘要、精确来源引用和来源组，原始对话只对少量高价值证据按需读取净化、限长的临时片段；
- 正式画像洞察只消费全局用户档案对会话聚合维度的再抽象结果，必须由至少两个真正独立的学习会话支持并检查限制性/反向证据；同一会话的阶段与最终 Review 不得重复计权，课程总 Review 和周报也不得回写为新的画像行为证据；
- 当前画像页不提供候选反馈或拒绝组件，不定义或记录画像拒绝事件；
- 规划/回顾左侧七天栏无内部滚动，七张卡等分铺满并粘性跟随；仅右侧内容超出时内部滚动；
- 课程表和学习日历显示具体课节名称；
- 周课程表与学习日历只显示有限课节预览；溢出时用“另有 N 节”承接，完整内容分别在下方当日列表和右侧日期详情展示，不在日期卡内滚动；
- 课程规划日期使用页面内的非模态日期选择层：紧贴触发按钮，优先向上展开、空间不足时向下展开；选日后立即更新排期，取消排期不需要二次确认。计划流每天使用统一目标时长，支持超过 90 分钟的 15 分钟步进自定义值，周预计时长随学习日和时长实时计算；
- 课程规划不保存可冲突的排期状态字段：仅对未完成、未放弃课节按“无日期 / 今天或未来 / 早于今天”实时显示为“待规划 / 已安排 / 已逾期”；日期只在右侧排期操作位出现，卡片信息流只显示课程名、预计时长、排期状态和主题标签；
- 周课程表与学习日历的日期卡内课节统一使用无背景、无边框的紧凑文字行；周历课节名称为 `9px`、行高 `1`、内边距 `6px`，星期与日期层级不变；日期卡外层底色和选中态仍承担状态表达；
- 正式课程大纲右侧采用单一粘性信息栏：进度为紧凑摘要，推荐课节是唯一强调区，版本记录与尚不可用的主题总结降级为轻量入口；移动端回到自然单列；
- 统一设计系统页面已接入共享运行状态层；AI 接口与本地服务两个状态可点击进入运行中心对应页签，故障场景和自愈结果跨样稿同步；
- 共享运行状态入口必须以 `runtime-status.js` 自身 URL 为基准固定 UI 根目录，不得在点击时借用当前页面层级推导路径；两个状态入口使用真实 `href`，以便静态与运行时共同校验；
- 课节记录只切换和展示已有原始/补充会话，不提供发起补充学习按钮；
- 存在补充学习时，在“学习对话”内部显示会话列表，不新增顶层 Tab；
- 最终 Review 和课程总 Review 均无编辑或重新生成入口。

本轮 UI 验收基线（2026-07-13）：27 个 HTML 在桌面、平板、移动端共 81 次渲染中，控件几何、模块几何、文字排版、间距边界与严格视觉完整性均零失败；25 个产品页纯度、27 个页面加载/控件接线和 22 组关键交互路径通过。详细证据见 `docs/设计文档/UI全页面控件交互验收矩阵.md`。

## 9. 教学质量参考

`docs/教学范例集` 包含数学、哲学、计算机、经济学、艺术与设计、社会科学、历史和商业管理样例。它们用于帮助定义高质量教学形态和 Review 输出，不是要固化成统一模板。

教学设计应提取案例中的机制：

- 如何建立问题链；
- 如何根据回答调整教学；
- 如何收集掌握证据；
- 如何形成知识结构；
- 如何基于真实互动评价学习表现。

不要机械复制章节结构或措辞。

## 10. 已确认的课程永久删除规则

- `DeleteCourse` 是独立于课程关闭的不可逆级联命令；不支持归档、软删除、回收站或恢复。
- 正式课程大纲页提供独立危险入口；风险弹窗只有“取消”和“永久删除”，不要求输入课程名称。
- 删除先撤销该课程全部会话写入权并停止计时，再移除课程档案、大纲、课节、会话、Review、排期、计划流、材料引用和 UI 索引。
- 同时撤销课程贡献的统计事实、日历条目、画像候选证据与来源组；从剩余事实重建统计并触发学习画像重算。
- 画像重算失败不回滚删除；已删课程不得继续出现在任何统计、日历、画像证据或画像结论中。

详见《课程永久删除功能规则》。

## 11. 正在设计与明确后置

- 已确认并已制作样稿：八个玩法壳的整体玩法意图、自由度边界、阅读材料特例，以及“主页九模式入口 + 全宽统一建档工作台”的整合方式；
- 已确认并已分类制作：UI 样例按功能域与课程生命周期组织；九模式视觉身份采用渐进式延续；最终课时 Review 成功后展示完整 Markdown 弹窗；
- 历史功能域使用历史统计、学习日历、学习画像三个同级 Tab 与三个视图；历史课程采用历史统计页内的清单式回顾，不再提供独立课程聚合历史页面；
- 历史统计页面已确认以 A“分层归档”为深化方向，采用顶部核心事实、中部趋势与分布、底部课程清单；课程清单上方直接排布课程状态、学科、来源模式与排序下拉项，学科和模式选项可持续扩展，不提供搜索框、主题筛选或总筛选按钮；页面只展示可复算事实，字段、指标和数据流向由《历史统计与学习画像数据源定义清单》统一定义；
- 历史统计与学习画像的数据源、字段、统计口径、全局学习档案、候选证据、定向原文、复合证据链、生成刷新、隐私和前端分流已经分别写入《历史统计与学习画像数据源定义清单》《学习画像数据抓取与分析策略规则》；
- 学习画像视觉结构已形成当前样稿：整体沿用共享设计系统，顶部直接呈现 AI 自由生成的标题与摘要，下方使用统一的“编号栏—自由正文—证据链折叠栏”组件并保留可继续生成占位；
- 已确认可信度原则：正式画像洞察绝不能由单点证据直接生成，必须综合至少两个具有一定独立性的跨课节、跨课程或跨时间来源组，并纳入限制性/反向证据；单点事实只能作为候选观察；
- 通用课程及其他玩法的附件、网页和外部资料规则；阅读研讨的 PDF/TXT/Markdown 上传与章节映射已纳入玩法规则；
- 推荐扩展课程点击后的创建流程；
- 生产级 Prompt、模型选择和 Provider 协议；
- 是否未来支持云同步、多用户或登录体系。

这些后置模块不能反向改变已确认的课程状态机和 Review 不可变原则；如确需改变，必须先更新专项规格和领域词汇表。

## 12. 建议的下一阶段

书面架构与详细实施计划均已获批准。下一步按 [Learning MORE 详细实施计划](<./docs/架构方案/Learning MORE 详细实施计划.md>) 顺序执行：

1. 执行阶段 01，初始化 Git、pnpm Monorepo、共享合同、架构检查、测试工具和健康纵向切片；
2. 通过阶段 01 Phase Gate 后再进入文件持久化与 GenerationRuntime；
3. 后续严格按 03–08 的纵向切片和门禁推进，不跨过未通过的恢复/等价检查；
4. 每完成一条回归断言，将 `equivalence-matrix.yaml` 对应项从 `unimplemented` 改为 `passing`，但必须同时存在实际通过的测试；
5. 任何改变四状态生命周期、Review 不可变、课程关闭顺序、来源组或画像证据规则的实现提议，必须先回到专项规格与架构文档重新批准。

开始编码前只需获得“执行实施计划”的明确指令；不再重复设计已批准架构。

## 13. 新对话接手检查清单

接手后先确认：

- 当前任务默认是按实施计划开始编码；如要修改架构，必须明确指出要重开哪项已批准决定；
- 用户是否已经批准《课程学习与 Review 功能逻辑规则》；
- 是否有新口径推翻本文内容；
- 改动是否同步影响 `CONTEXT.md`、专项规则、回归基线和 UI sample；
- 是否正在误用旧项目字段、思维模式、灵感放送或旧课程 Prompt。

任何新的领域决定都应同时检查四处：

1. `CONTEXT.md` 领域词汇；
2. 对应专项功能规则；
3. 基础模块回归基线；
4. UI 视觉样例与清单。

## 14. 建议使用的 Codex skills

- `brainstorming`：继续定义功能边界或修改行为前；
- `domain-modeling`：新增或推翻领域术语、状态和不变量时；
- `prototype`：需要验证页面结构或状态交互时；
- `writing-plans`：规格批准后生成详细实施计划；
- `tdd`：正式实现状态机、Repository 和回归测试时；
- `diagnosing-bugs`：新旧行为回归或服务自愈出现异常时。

## 15. 上下文同步维护规则

本对话及后续对话中，只要出现会影响开发语境的已确认决定、阶段进度、废弃口径、文档入口或下一步重点，必须同步更新本文件。领域术语同步更新 `CONTEXT.md`；详细行为同步更新对应专项规则和回归基线；涉及页面的决定同步更新 UI 清单与最终 sample。

尚未确认的构想只能标记为“正在讨论”或“候选方案”，不得写成既定事实。后续接手者应先读取本文件的更新时间和“当前阶段”，再进入专项文档。

## 16. 2026-07-13 UI 全量审计进度

本轮 UI 样稿审计已经闭环，当前权威证据为：

- `docs/设计文档/UI全页面控件交互验收矩阵.md`：27 页状态均为“通过”；
- `docs/UI视觉预览/00-设计系统/tests/run-control-geometry.mjs`：27 页 × 3 视口控件几何通过；
- `run-module-geometry.mjs`：27 页 × 3 视口模块几何通过；
- `run-typography-spacing.mjs`：27 页 × 3 视口字号、行高、点击区域、内边距与文字边界通过；
- `run-visual-integrity.mjs`：27 页 × 3 视口严格视觉完整性通过，内置坏样例已验证可捕获控件变形、模块越界、文字行距与边距失衡；
- `run-control-wiring.mjs`：27 页无可见死按钮或无目标链接；
- `run-interaction-regression.mjs`：22 组关键前端模拟路径通过；
- `run-ui-audit.mjs`：25 个产品页纯度通过；`run-page-smoke.mjs`：27 页加载通过。

本轮实际修复了平板端课程规划与上周回顾七日栏裁切、计划流步骤热区过小、移动端历史统计筛选高度不足、运行中心状态胶囊和关闭键受挤压，以及平板端全局运行状态入口退化为无文字圆点的问题。当前视觉基线仍使用桌面 `1440×1000`、平板 `1024×768`、移动端 `390×844`。

这批 HTML 仍是交互与视觉决策样例，不是 React + TypeScript 生产实现。后续重构应把上述测试口径迁移为正式前端组件、共享领域合同和端到端回归，而不是直接复制样稿内部代码。
